# ElevenLabs API key for Backing Track (Music generation).
# Get a key at https://elevenlabs.io — used only by the frontend for backing tracks.
# Copy this file to .env and replace with your real key (never commit .env).
VITE_ELEVENLABS_API_KEY=your_elevenlabs_api_key

# Spotify Web Playback (Connect with Spotify in the app).
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id
# Optional: set to the exact Redirect URI you added in Spotify Dashboard (e.g. http://127.0.0.1:1420/ for Tauri).
# VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:1420/
